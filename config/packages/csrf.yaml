# Stateless (double-submit-cookie) CSRF for storefront forms that ship with the
# Stimulus csrf controller. Admin login/logout use classic session CSRF so they
# work without JavaScript.
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        stateless_token_ids:
            - submit
