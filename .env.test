# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'

# Tests run inside DDEV against the MariaDB service. The "_test" suffix is added
# by config/packages/doctrine.yaml (when@test). Override in .env.test.local to
# run the suite outside DDEV.
DATABASE_URL="mysql://db:db@db:3306/db?charset=utf8mb4&serverVersion=10.11.0-mariadb"
LEGACY_DATABASE_URL="mysql://db:db@db:3306/legacy_central?charset=utf8mb4&serverVersion=10.11.0-mariadb"
